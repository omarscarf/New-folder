import React from 'react';

const Header: React.FC = () => {
    return (
        <header>
            <h1>Chat App</h1>
            {/* Navigation items will go here */}
        </header>
    );
};

export default Header;